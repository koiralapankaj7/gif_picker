/// Defines the breakpoints for the UI.
abstract class Breakpoints {
  /// Max width for a small layout (Mobile).
  static const double small = 480;

  /// Max width for a medium layout (Tab).
  static const double medium = 720;

  /// Max width for a medlargeium layout (Web, desktop).
  static const double large = 1200;

  /// Max width for a extra large layout (Monitor, TV).
  static const double xLarge = 1440;
}
