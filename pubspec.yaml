name: gif_picker
description: A Very Good Project created by Very Good CLI.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  dio: ^4.0.4
  flutter:
    sdk: flutter
  flutter_staggered_grid_view: ^0.5.1
  logging: ^1.0.2
  slidable:
    git:
      url: git://github.com/koiralapankaj7/slidable.git
      ref: main

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^2.4.0
